#include <stdio.h>
#include <math.h>
#include <string.h>

int fact(int n);
int main()
{
   

    
    
   
           

    char new[] = {"new world"};
    char old[] = {"old world"};

    strcpy(new, old); // the value of old is replacing the value of new

    puts(new);
    // puts(old);
    printf("%s", old);
}