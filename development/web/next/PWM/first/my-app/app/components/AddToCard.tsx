'use client'
import React from 'react'

const AddToCard = () => {
  return (
    <div>
      <button onClick={()=>console.log("added")} > add to card </button>
    </div>
  )
}

export default AddToCard
